import React from "react";
import classes from "../../../main.module.css";

const Logo = () => {
  return (
    <div className={classes.logo}>
      <a href="/#welcome">MC</a>
    </div>
  );
};

export default Logo;
